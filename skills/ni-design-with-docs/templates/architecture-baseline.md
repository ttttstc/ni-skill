# {方案名称}产品与架构设计

{排版提示：全文按“先说结论，再说依据和细节”的顺序写。优先使用短段落和分块。默认保留 1–2 张表，最多 3 张；表格只用于短文本的横向比较、映射或状态汇总。复杂交互、模块协作、状态变化和跨系统数据流按需使用图。图帮助理解，不能代替关键决定和协作约束的文字结论。花括号内容是生成提示，正式报告必须删除。不得把提示改写成“本章”“下文”“前文”或“唯一真源”等元话语。}

## 1. 方案摘要

{只写一个段落，使用 2–3 个短句。第一句说最终要做到什么。第二句说系统采用什么核心做法，以及关键失败或安全问题怎么处理。第三句说主要改哪里、哪些部分保持不变。不要写背景、过程、术语解释和空泛目标。}

## 2. 目标和范围

{先用一句话说清本次需求要解决的具体问题和交付边界。}

### 2.1 背景与要解决的问题

{先写当前事实，再写直接影响和本次变化。背景只保留与本次设计直接相关的内容。不要重复系统边界和责任，不要写访谈状态、作者推理、被否决方案、报告策略或“帮助研发、统一认知、建设能力、便于后续开发”等文档用途。“研发”只有在承担实际责任时才出现，不作为读者或受益者。}

### 2.2 做到什么算完成

### 2.3 这次做什么

### 2.4 这次不做什么

{只写用户已经确认、且容易被误认为本次范围的业务或产品内容；没有则省略。不要写 Skill 不处理的实现细节。}

### 2.5 当前与目标差异（仅增量需求）

{仅当已有能力的当前行为与目标行为之差会影响研发理解或总体设计时保留；全新能力、没有有意义现状或差异不影响设计时删除整个小节。当前行为必须来自用户确认或可靠资料，不补写未知现状。}

| 能力 | 当前行为 | 目标行为 | 设计影响 |
|---|---|---|---|
| {与本次需求直接相关的能力} | {已确认的当前行为} | {本次要达到的行为} | {需要解决的设计问题，不提前创造模块或实现} |

{简单变化可以用短段落代替表格。只写本次变化，不展开现状教程，也不重复后文的产品方案和架构设计。正式成稿时按实际保留的小节重新编号。}

## 3. 总体设计

{本章按“总体结论 → 产品方案 → 系统边界和责任 → 流程、数据和状态 → 系统规格 → 关键决定和协作约束”展开。每节先写一句结论，再补支撑信息。}

### 3.1 总体设计结论

{用 3–5 个短句说清整体方案：系统最终怎么工作，主要改变哪些责任或关系，哪些部分保持不变，最重要的失败或安全保护是什么。不要从接口、数据结构或实现步骤讲起。}

### 3.2 产品方案

{先用一句话概括产品面向谁、解决什么问题以及用户最终能得到什么。}

#### 面向谁和解决什么

#### 从哪里进入，怎样完成任务

{写清入口、主要操作和系统反馈。涉及多角色、多状态或分支时，使用一张用户流程图。不要再把同一条用户路径拆成多组“产品场景”。}

#### 用户会看到什么结果

{按需说明处理中、成功、失败、等待、超时和取消后的状态。没有取消能力时直接说明。}

#### 支持什么，不支持什么

{只写用户或调用方需要知道的产品边界，不重复项目范围。}

### 3.3 系统边界、影响范围与责任

{先用一句话说明本方案由谁承担核心责任，以及责任在哪里结束。}

#### 系统负责什么

{根据已经确认的访谈输入，由设计者归纳系统负责交付的结果和停止承担责任的位置。不要通过猜测或代码扫描补全内部现状。}

#### 影响哪些人和系统

- **{对象}**：{与本需求的关系}；**本次影响**：{新增责任 / 调整协作 / 保持不变 / 外部约束}

{只列理解总体方案必需的用户、系统和直接依赖。没有影响的部分不列。}

#### 谁负责什么

- **{对象}**：{本次需求中的主要责任}

{有两个以上系统或模块时，附一张简化的模块架构关系图。图只画已经确认的对象、责任和关键关系。即使图中已经展示，影响开发的责任结论也必须在正文中写明。}

### 3.4 主要流程、数据与状态

{先用一句话说清主流程如何把产品方案变成结果。正文只补关键阶段、分支和结果；普通步骤不要和图逐项重复。流程先展示各部分怎样协作，后面的系统规格集中列出必须遵守的规则。}

#### 主要流程

{跨角色或跨模块协作时，使用 UML 时序图或 UML 活动图。图回答“谁在什么时候完成什么”。简单单一责任方的流程可以只写短步骤，但要说明不画图的理由。}

#### 核心数据

{只写会影响数据所有权、跨系统传递、一致性、安全或验收的数据。传递契约写在“系统规格”中；本节只说明数据来源、流向、所有者和检查点，并按需附数据流图。不要设计表和字段。}

#### 核心状态

{存在会影响用户结果、恢复或协作的状态变化时，附 UML 状态图，说明状态、进入条件和离开结果。没有关键状态变化时直接说明。}

### 3.5 系统规格

{先用一句话概括系统必须遵守的核心规则。本节是系统行为和约束的唯一真源，回答“系统必须做到什么”。简单规则直接用自然语言；不新增独立 Story、行为规格或 Gherkin 章节，不重复用户操作和验收步骤，也不写接口字段、内部结构或实现步骤。复杂条件只有在更清楚时才按需使用 Given / When / Then。}

- **核心规则**：{系统在什么条件下必须做什么}
- **权限和数据规则**：{谁能做什么；哪些数据可以读取、传递或保留}
- **状态和结果规则**：{什么状态或结果才算成功、等待、失败、超时或取消}
- **失败时必须遵守的规则**：{哪些失败必须停止、拒绝、重试、降级或等待处理}
- **硬限制**：{会改变总体方案的层数、大小、容量或兼容边界；没有依据时不编造数字}

{只保留与本次方案有关的条目。没有内容的条目删除。}

#### 输入、上下文、凭证、Secret 和结果传递（有跨边界传递时保留）

{对适用的输入、调用参数、上下文、Token、Secret、输出、状态和错误，逐项写清来源和接收方、作用域、映射、继承或覆盖、转换、优先级、校验、默认值、缺失或非法时的结果，以及权限、日志、存储、生命周期、撤销、超时、重试和可观察结果。不要只画箭头或写“自动继承”。不适用的项目说明原因，未知项回到访谈或对齐阶段。这里的规则是正文唯一依据，流程图和数据流图只帮助理解。}

### 3.6 关键决定与协作约束

{先用一句话概括哪些决定和共同规则最可能影响返工、系统协作或上线。正文是权威结论，图只帮助理解，不能成为这些内容的唯一载体。}

#### 关键决定

##### 决定 1：{一句话结论}

- **结论**：{决定采用什么}
- **理由**：{为什么这项选择最直接地满足目标和约束}

{只写会改变系统边界、长期协作、安全、质量、上线方式或返工范围的决定。通常保留 3–5 项；没有结构性选择时可以省略。}

#### 协作约束

##### 约束 1：{一句话说明必须共同遵守的规则}

- **规则**：{必须做什么或禁止做什么}
- **涉及对象**：{系统、模块或团队}
- **生效阶段**：{在流程的哪个阶段执行}
- **违反后**：{系统停止、拒绝、等待、降级或进入什么状态}

{只保留跨系统、跨团队或长期存在的规则。即使结构图或流程图已经展示，也要在这里写出会影响开发的约束结论；不必复述普通流程步骤。}

## 4. 质量、安全和异常处理

{本章只补充前文没有完整表达、且会影响总体方案的保障要求。同一个问题只放在一个主要位置；其他章节只引用，不重复解释。}

### 4.1 关键质量要求

{先用一句话概括最影响整体设计的质量要求。只写会改变总体设计的速度、容量、稳定性、恢复、数据正确性、问题定位或兼容要求。}

#### {具体质量要求}

- **发生什么**：{触发条件和所处场景}
- **系统怎么处理**：{系统必须怎样响应，以及用户或调用方看到什么}
- **怎样判断通过**：{通过什么测试、演练或可读取结果判断}

{不要写“高性能、高可靠”。没有依据时不编造数字；数值不改变总体方案时留给详细设计或容量测试。不要重复安全问题或关键失败。没有相关要求则省略。}

### 4.2 安全问题和解决办法

{先用一句话概括本次最重要的安全问题和控制结论。安全影响不显著时，只用一句话说明原因。}

#### {具体安全问题}

- **问题和后果**：{谁能从什么入口做什么；可能造成什么后果}
- **系统怎么处理**：{控制落在哪一步、由谁执行、失败后是否停止}
- **验收标准**：{通过什么越权、泄露、重放、资源或回退测试判断通过}

{只写与本次改动有关的攻击、滥用、泄露、越权和安全控制。一般故障放到 4.3。}

### 4.3 关键失败与恢复

{先用一句话概括哪些失败需要单独的恢复路径。只写主流程、质量或安全章节没有完整说明的异常。}

#### {关键失败}

- **用户或调用方看到**：{失败、等待、部分完成或未知状态}
- **系统怎么处理**：{停止、重试、降级、回退或等待人工处理}
- **如何恢复**：{恢复条件、恢复动作和负责人}

{不要罗列所有异常码，也不要重复主流程中的普通分支。}

## 5. 上线与验收

{先用一句话概括怎样安全上线，以及用哪些结果判断方案已经实现。}

### 5.1 如何上线和回退

{按需说明新旧行为如何共存、如何放量、何时暂停或回退、谁负责执行，以及哪些步骤不能直接回退。没有兼容、迁移或灰度影响时，直接说明。}

### 5.2 如何验收

{验收只验证前文结论，并定义研发和测试必须覆盖的设计级主场景；不重新描述方案，也不展开等价类、组合矩阵、测试数据或自动化实现。按适用性选择核心成功路径、关键边界、核心异常、权限与安全、兼容回归和架构级质量要求。}

#### AC-xx {能够表达验收结论的标题}

- **覆盖要求**：{对应的系统规格、质量、安全、兼容或上线要求}
- **前置条件**：{需要构造的状态、输入或环境}
- **操作**：{执行的关键动作}
- **通过条件**：
  - {状态、数量、已确认阈值、一致性或其他确定性结果}
  - {必要的第二个可观察结果}
- **验证方式**：{仅在不显而易见时填写，例如端到端测试、故障注入、安全测试、基准测试、回归测试或人工核验}

{简单方案可不用 SR/AC 编号，但每个主场景仍须包含覆盖要求、前置条件、操作和明确通过条件。不要只写“正确执行、正常运行、合理失败、体验良好、基本兼容、高性能、高可用、符合预期”。数值必须来自产品目标、已有基线、官方标准或用户确认；没有依据时不得编造。}

## 6. 风险与参考来源

{先用一句话概括仍可能影响开发、上线或后续设计的事项。}

### 6.1 风险、待确认和后续详细设计

#### {事项}

- **当前结论**：{已知事实或仍未确定的内容}
- **影响**：{不处理会影响什么}
- **下一步**：{由谁在什么阶段确认、设计或验证}

{这里只放仍未解决、需要后续行动的事项。不要重复已经解决的问题，也不要把所有实现细节机械列为待办。}

### 6.2 参考来源

- [{来源名称}]({链接})：{它直接支持了哪项事实、约束或决定}

{只保留直接支持最终决定的一手来源。访谈资料写资料名称和版本，不写访谈过程。没有外部来源时说明“本方案仅依据用户确认的输入”。}
