# {方案名称}产品与架构设计

{排版提示：正式报告优先使用短段落和分块。默认保留 1–2 张表，最多 3 张；表格只用于短文本的横向比较、映射或状态汇总。场景、质量、安全、失败和验收内容不要塞进宽表，改用“场景—适配策略—验收标准”分块。}

## 1. 方案摘要

{只写一个段落，使用 2–3 个短句。第一句说最终要做到什么。第二句说系统采用什么核心做法，以及关键失败或安全问题怎么处理。第三句说主要改哪里、哪些部分保持不变。不要写背景、过程、术语解释和空泛目标。}

## 2. 目标和范围

### 2.1 要解决的问题

{直接写具体需求和用户或系统要得到的结果。不要把“帮助研发、统一认知、建设能力、便于后续开发”当作产品目标。}

### 2.2 做到什么算完成

### 2.3 这次做什么

### 2.4 这次不做什么

{只写用户已经确认、且容易被误认为本次范围的业务或产品内容；没有则省略}

## 3. 总体设计

{本章先说明产品规则和系统边界，再说明整体做法、责任、流程、数据和协作约束。它是研发理解方案的主入口。}

### 3.1 系统规格（产品规格）

{本节只写系统必须提供的产品行为，不写接口字段、内部结构或实现步骤。}

#### 3.1.1 产品对象和使用入口

{说明这项产品能力处理什么对象，面向谁，从哪里触发。}

#### 3.1.2 核心规则和结果

不要把多条产品规则塞进表格。每个主要场景使用一个短分块：

##### 产品场景 1：{一句话说明谁在什么情况下使用}

- **场景**：{触发者、入口和前提}
- **产品规则**：{系统必须遵守的规则}
- **用户或调用方看到**：{成功、等待、失败、超时或取消时的结果}

{这里写产品必须遵守的规则和结果，不写接口字段、内部结构或实现步骤。}

### 3.2 用户怎么使用

#### 3.2.1 谁会使用

#### 3.2.2 操作步骤和系统反馈

操作涉及多个角色、状态或分支时，使用用户流程图、UML 时序图、UML 活动图或 UML 状态图。若与第 3.7 节的主流程图表达同一件事，直接引用，不重复画图。

按需写清处理中、成功、失败、超时和取消后的用户状态。没有取消能力时也要直接说明。

#### 3.2.3 失败后用户能做什么

### 3.3 系统边界与影响范围

#### 3.3.1 系统负责什么

{根据已经确认的访谈输入，由设计者归纳本方案负责交付的结果和停止承担责任的位置。不要要求用户提供架构图，也不要通过猜测或代码扫描补全内部现状。}

#### 3.3.2 涉及哪些人和系统

按短段落列出直接相关的对象：

- **{对象}**：{与本需求的关系}；**本次影响**：{新增责任 / 调整协作 / 保持不变 / 外部约束}

只写理解总体方案必需的用户、系统和直接依赖。没有影响的部分不列。

### 3.4 整体做法

用 3–6 个短句说明系统采用什么总体思路，各部分如何形成完整结果，以及最关键的失败保护。不要先讲接口和实现步骤。

### 3.5 核心设计选择

| 要决定什么 | 结论 | 为什么这样做 |
|---|---|---|
| ... | ... | ... |

只写会改变系统边界、长期协作约束、安全、质量、上线方式或返工范围的决定。通常保留 3–5 项；没有结构性选择时可以省略本节。

### 3.6 谁负责什么

- **{对象}**：{本次需求中的主要责任}

当有两个以上系统或模块时，附一张“模块架构关系图”，说明各对象如何协作以及本次改变了哪条关系。只使用已经确认的对象，不把它画成无关的分层图。

### 3.7 主要流程

用短步骤说明主流程，并附至少一张 UML 时序图或 UML 活动图，回答“谁在什么时候完成什么”。图中只保留会影响不同对象协作、状态或失败处理的步骤。简单单一责任方的流程可以只用短步骤，但必须说明不画 UML 图的理由。

存在并发、重复、乱序或异步结果合并时，按需写清它们是否会改变用户结果、数据状态或恢复方式。

### 3.8 核心数据和状态

只写会影响系统边界、数据所有权、状态转换、一致性、安全或验收的内容；不设计表、字段和内部数据结构。

- 存在状态变化时，附 UML 状态图，说明状态、进入条件和离开结果；
- 数据跨越模块或系统边界时，附数据流图，说明数据从哪里来、经过谁、写到哪里以及在哪一步检查；
- 同时存在状态变化和跨边界数据流时，两张图都要有；图可以复用正文中已经回答同一问题的图；
- 没有状态变化或跨边界数据时，直接说明不需要对应图。

### 3.9 关键协作约束

#### 约束 1：{一句话说明必须共同遵守的规则}

- **涉及对象**：{系统、模块或团队}
- **违反后**：{系统停止、拒绝、等待、降级或进入什么状态}

只保留跨系统、跨团队或长期存在的规则，不复述“谁负责什么”，不展开接口字段。没有则省略。

## 4. 系统必须达到的质量要求

质量要求使用分块，不使用三列表格。每个分块都要回答“发生什么、系统怎么应对、怎么验收”。

### 质量场景 1：{一句话说明触发情况}

- **场景**：{触发条件和所处场景}
- **适配策略**：{系统必须怎样响应，以及用户或调用方看到什么}
- **验收标准**：{通过什么测试、演练或可读取结果判断通过}

只写会改变总体设计的速度、容量、稳定性、恢复、数据正确性、问题定位或兼容要求。不要写“高性能、高可靠”。没有依据时不编造数字；数值不改变总体方案时留给详细设计或容量测试。没有相关要求则省略。

## 5. 安全问题和解决办法

存在安全问题时，直接说明问题和处理办法：

### 安全场景 1：{一句话说明具体安全问题}

- **场景**：{谁在什么入口做什么；如果发生，可能造成什么后果}
- **适配策略**：{控制落在哪一步、由谁执行、失败后是否停止}
- **验收标准**：{通过什么越权、泄露、重放、资源或回退测试判断通过}

存在跨系统、跨租户、外部输入或敏感数据流动时，按需在第 3.8 节增加数据流图。安全影响不显著时，只用一句话说明原因。

## 6. 出错怎么办

### 失败场景 1：{一句话说明失败点}

- **场景**：{哪里失败，用户或调用方处于什么状态}
- **适配策略**：{系统如何停止、重试、降级或等待人工处理；用户或调用方看到什么}
- **验收标准**：{如何确认没有误报成功、错误状态可定位、恢复路径可用}

只列会影响用户结果、数据正确性或系统协作的失败。

## 7. 如何上线和回退

存在兼容、迁移、灰度或回退要求时保留本节；否则省略。

写清什么信号触发暂停或回退，以及谁负责决定和执行。

## 8. 如何验收

### 验收场景 1：{一句话说明要验证的结果}

- **场景**：{正常路径、边界、失败、权限、兼容或回退场景}
- **操作前提**：{准备什么，执行什么操作}
- **验收标准**：{能够观察和判断的预期结果}

结果必须可以观察和判断。

## 9. 风险、待确认和后续详细设计

### 风险 1：{一句话说明风险或待确认事项}

- **事项**：{还缺什么事实或决定}
- **影响**：{对详细设计、开发、上线或回退有什么影响}
- **下一步**：{负责人和关闭方式}

只列会影响总体方案、详细设计启动或上线的事项。后续栏目只写下一阶段必须解决的问题，不代替研发设计接口和内部实现。

## 10. 参考来源

只列正文中实际支撑关键事实、限制或决定的一手来源。
